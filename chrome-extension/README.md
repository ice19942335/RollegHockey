# Chrome Extension: RollegHockey Keep Alive

Расширение для Chrome, которое автоматически открывает сайт RollegHockey для предотвращения засыпания Supabase на бесплатном плане.

## Установка

1. Откройте Chrome и перейдите по адресу `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `chrome-extension` из этого проекта
5. Расширение будет установлено и готово к использованию

## Использование

### Настройка через Popup

1. Нажмите на иконку расширения в панели инструментов Chrome
2. Откроется окно настроек

### Параметры настройки

- **Включено**: Включить/выключить автоматическое открытие сайта
- **URL сайта**: Адрес сайта для открытия (по умолчанию: `https://ice19942335.github.io/RollegHockey/`)
- **Режим расписания**:
  - **Ежедневно в указанное время**: Открывает сайт каждый день в заданное время
  - **Каждые N часов**: Открывает сайт через заданный интервал времени
- **Время открытия**: Время для ежедневного режима (формат: ЧЧ:ММ)
- **Интервал (часы)**: Количество часов между открытиями для интервального режима
- **Закрыть вкладку через (секунды)**: Автоматически закрыть вкладку через указанное количество секунд (0 = не закрывать)
- **Открывать при запуске браузера**: Автоматически открывать сайт при запуске Chrome

### Кнопки

- **Сохранить**: Сохраняет текущие настройки
- **Запустить сейчас**: Открывает сайт немедленно (с проверкой "раз в день")
- **Force Run**: Открывает сайт немедленно, игнорируя проверку "раз в день"

## Как это работает

1. Расширение использует Chrome Alarms API для планирования открытия сайта
2. При срабатывании будильника открывается новая вкладка с указанным URL
3. Если включено, вкладка автоматически закрывается через заданное время
4. Для ежедневного режима расширение отслеживает дату последнего открытия, чтобы не открывать сайт дважды в один день

## Технические детали

- **Manifest Version**: 3
- **Permissions**: `alarms`, `tabs`, `storage`
- **Service Worker**: `background.js` - обрабатывает будильники и открытие вкладок
- **Popup**: `popup.html` и `popup.js` - интерфейс настроек

## Разработка

### Структура файлов

```
chrome-extension/
├── manifest.json      # Манифест расширения
├── background.js      # Service Worker (фоновая логика)
├── popup.html         # HTML интерфейса настроек
├── popup.js           # JavaScript логики настроек
├── README.md          # Эта инструкция
└── icon*.png         # Иконки расширения (16x16, 48x48, 128x128)
```

### Обновление расширения

1. Внесите изменения в файлы
2. Перейдите на `chrome://extensions/`
3. Нажмите кнопку обновления (🔄) рядом с расширением

## Примечания

- Расширение работает только когда Chrome запущен
- Для ежедневного режима используется локальное время браузера
- Вкладки открываются в фоновом режиме (не активными)
- Расширение сохраняет настройки локально в Chrome Storage

## Решение проблем

### Расширение не открывает сайт

1. Проверьте, что расширение включено в `chrome://extensions/`
2. Убедитесь, что настройка "Включено" активна в popup
3. Проверьте правильность URL в настройках
4. Попробуйте использовать кнопку "Force Run"

### Вкладка не закрывается автоматически

1. Убедитесь, что значение "Закрыть вкладку через" больше 0
2. Проверьте, что вкладка не была закрыта вручную до истечения времени

### Настройки не сохраняются

1. Убедитесь, что вы нажали кнопку "Сохранить"
2. Проверьте консоль браузера на наличие ошибок (F12)
