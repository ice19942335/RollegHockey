# 🚀 Полная инструкция по деплою на GitHub Pages

Эта инструкция поможет вам развернуть приложение "Турнирная таблица по хоккею" на GitHub Pages.

## 📋 Предварительные требования

- Аккаунт на GitHub
- Git установлен на вашем компьютере
- Node.js и npm установлены (версия Node.js 18 или выше)

## 🔧 Шаг 1: Подготовка репозитория

### 1.1. Убедитесь, что код загружен в GitHub

Если вы еще не загрузили код в репозиторий:

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте удаленный репозиторий
git remote add origin https://github.com/ice19942335/RollegHockey.git

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit"

# Загрузите в GitHub
git push -u origin main
```

### 1.2. Проверьте конфигурацию

Убедитесь, что в файле `vite.config.js` указан правильный `base` путь:

```javascript
base: process.env.NODE_ENV === 'production' ? '/RollegHockey/' : '/',
```

**Важно:** Путь `/RollegHockey/` должен соответствовать названию вашего репозитория на GitHub.

## 🎯 Шаг 2: Настройка GitHub Pages

### 2.1. Включите GitHub Pages в настройках репозитория

1. Откройте ваш репозиторий на GitHub: https://github.com/ice19942335/RollegHockey
2. Перейдите в **Settings** (Настройки)
3. В левом меню найдите раздел **Pages**
4. В разделе **Source** выберите:
   - **Source**: `GitHub Actions` (не "Deploy from a branch")
5. Нажмите **Save** (Сохранить)

### 2.2. Проверьте, что workflow файл создан

Убедитесь, что файл `.github/workflows/deploy.yml` существует в вашем репозитории. Если его нет, создайте его (см. ниже).

## ⚙️ Шаг 3: Автоматический деплой через GitHub Actions (Рекомендуется)

### 3.1. Workflow уже настроен

В проекте уже есть файл `.github/workflows/deploy.yml`, который автоматически:
- Собирает проект при каждом push в ветку `main`
- Деплоит на GitHub Pages

### 3.2. Загрузите workflow файл в репозиторий

Если файл `.github/workflows/deploy.yml` еще не загружен:

```bash
# Добавьте файл
git add .github/workflows/deploy.yml

# Создайте коммит
git commit -m "Add GitHub Actions workflow for deployment"

# Загрузите в GitHub
git push origin main
```

### 3.3. Проверьте деплой

1. После push в ветку `main` перейдите в раздел **Actions** вашего репозитория
2. Вы увидите запущенный workflow "Deploy to GitHub Pages"
3. Дождитесь завершения (обычно 2-3 минуты)
4. При успешном деплое появится зеленая галочка ✅

### 3.4. Доступ к сайту

После успешного деплоя ваш сайт будет доступен по адресу:
```
https://ice19942335.github.io/RollegHockey/
```

**Примечание:** Первый деплой может занять до 10 минут. Последующие деплои происходят быстрее.

## 🔨 Шаг 4: Альтернативный способ - Ручной деплой

Если вы предпочитаете деплоить вручную:

### 4.1. Установите зависимости

```bash
npm install
```

### 4.2. Установите gh-pages (если еще не установлен)

```bash
npm install --save-dev gh-pages
```

### 4.3. Выполните деплой

```bash
npm run deploy
```

Эта команда:
1. Соберет проект (`npm run build`)
2. Загрузит папку `dist` в ветку `gh-pages`

### 4.4. Настройте GitHub Pages для ручного деплоя

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)`
3. Нажмите **Save**

## ✅ Шаг 5: Проверка работы сайта

### 5.1. Проверьте локально перед деплоем

```bash
# Соберите проект
npm run build

# Запустите preview
npm run preview
```

Откройте `http://localhost:4173` в браузере для проверки.

### 5.2. Проверьте на GitHub Pages

После деплоя откройте:
```
https://ice19942335.github.io/RollegHockey/
```

## 🔄 Обновление сайта

### При использовании GitHub Actions (автоматический деплой):

1. Внесите изменения в код
2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```
3. GitHub Actions автоматически соберет и задеплоит обновления

### При использовании ручного деплоя:

```bash
npm run deploy
```

## 🐛 Устранение проблем

### Проблема: Сайт показывает 404 или пустую страницу

**Решение:**
1. Проверьте, что `base` в `vite.config.js` соответствует названию репозитория:
   ```javascript
   base: process.env.NODE_ENV === 'production' ? '/RollegHockey/' : '/',
   ```
2. Убедитесь, что путь заканчивается на `/` (слеш)
3. Подождите 5-10 минут после первого деплоя

### Проблема: CSS и JS файлы не загружаются (белый экран)

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что `base` путь правильный
3. Очистите кэш браузера (Ctrl+Shift+R)
4. Проверьте, что все файлы загружены в ветку `gh-pages` или через Actions

### Проблема: GitHub Actions не запускается

**Решение:**
1. Убедитесь, что файл `.github/workflows/deploy.yml` существует
2. Проверьте, что вы пушите в ветку `main`
3. Перейдите в **Settings** → **Actions** → **General** и убедитесь, что Actions включены
4. Проверьте вкладку **Actions** на наличие ошибок

### Проблема: Workflow завершается с ошибкой

**Решение:**
1. Откройте вкладку **Actions** в репозитории
2. Кликните на неудачный workflow
3. Изучите логи ошибок
4. Частые причины:
   - Ошибки в коде (проверьте локально `npm run build`)
   - Неправильный `base` путь
   - Проблемы с зависимостями

### Проблема: Изменения не отображаются на сайте

**Решение:**
1. Подождите 2-3 минуты (деплой может занять время)
2. Очистите кэш браузера (Ctrl+Shift+R или Ctrl+F5)
3. Проверьте, что коммит был запушен в ветку `main`
4. Проверьте статус деплоя в разделе **Actions**

## 📝 Дополнительная информация

### Структура проекта после деплоя

```
RollegHockey/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions workflow
├── src/                         # Исходный код
├── dist/                        # Собранный проект (создается при build)
├── vite.config.js              # Конфигурация Vite
└── package.json                # Зависимости и скрипты
```

### Полезные команды

```bash
# Локальная разработка
npm run dev

# Сборка проекта
npm run build

# Просмотр билда локально
npm run preview

# Деплой на GitHub Pages (ручной)
npm run deploy
```

### Настройка кастомного домена (опционально)

Если у вас есть собственный домен:

1. Добавьте файл `CNAME` в папку `public/` с вашим доменом:
   ```
   yourdomain.com
   ```
2. Настройте DNS записи у вашего регистратора домена
3. В настройках GitHub Pages добавьте ваш домен

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:
```
https://ice19942335.github.io/RollegHockey/
```

При каждом push в ветку `main` будет происходить автоматический деплой (если используется GitHub Actions).

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи в разделе **Actions**
2. Убедитесь, что все шаги выполнены правильно
3. Проверьте документацию GitHub Pages: https://docs.github.com/en/pages

---

**Последнее обновление:** 2025
