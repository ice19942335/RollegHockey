# 🧪 Тестирование

Этот документ описывает, как запускать unit-тесты и UI-тесты проекта.

## 📋 Обзор

Проект использует [Vitest](https://vitest.dev/) в качестве тестового фреймворка. Тесты покрывают:

**Бизнес-логику:**
- Расчет статистики команд (`calculateTeamStats`)
- Расчет турнирной таблицы (`calculateStandings`)

**UI компоненты:**
- `TeamLogo` - отображение логотипов команд
- `StandingsTable` - турнирная таблица
- `ConfirmModal` - модальное окно подтверждения
- `Header` - заголовок с переключателем языка

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск тестов

#### Режим watch (автоматический перезапуск при изменениях)

```bash
npm test
```

#### Однократный запуск всех тестов

```bash
npm run test:run
```

#### Запуск с UI интерфейсом

```bash
npm run test:ui
```

Это откроет веб-интерфейс Vitest в браузере, где вы сможете:
- Видеть результаты тестов в реальном времени
- Фильтровать тесты
- Просматривать покрытие кода
- Отлаживать отдельные тесты

#### Запуск с покрытием кода

```bash
npm run test:coverage
```

## 📁 Структура тестов

```
src/
├── components/
│   ├── __tests__/
│   │   ├── TeamLogo.test.jsx       # Тесты для компонента логотипа
│   │   ├── StandingsTable.test.jsx  # Тесты для турнирной таблицы
│   │   ├── ConfirmModal.test.jsx   # Тесты для модального окна
│   │   └── Header.test.jsx         # Тесты для заголовка
│   └── ...
├── utils/
│   ├── __tests__/
│   │   └── calculateStats.test.js  # Тесты для расчета статистики
│   ├── calculateStats.js           # Бизнес-логика
│   └── googleSheets.js
└── test/
    └── setup.js                     # Настройка тестового окружения
```

## ✅ Покрытие тестами

### UI Компоненты

#### `TeamLogo`
- ✅ Отображение emoji логотипа
- ✅ Отображение дефолтного логотипа при пустом значении
- ✅ Отображение изображения по URL
- ✅ Корректная работа с разными размерами

#### `StandingsTable`
- ✅ Не рендерится при пустом списке команд
- ✅ Отображение команд и их статистики
- ✅ Корректные номера позиций
- ✅ Отображение легенды и системы очков
- ✅ Отображение логотипов команд
- ✅ Корректный знак разницы голов

#### `ConfirmModal`
- ✅ Не рендерится когда закрыт
- ✅ Отображение заголовка и сообщения
- ✅ Вызов onClose при нажатии на кнопку отмены
- ✅ Вызов onConfirm при нажатии на кнопку подтверждения
- ✅ Закрытие при клике на overlay
- ✅ Не закрывается при клике на содержимое модального окна

#### `Header`
- ✅ Отображение названия компании
- ✅ Отображение заголовка и подзаголовка
- ✅ Отображение кнопки переключателя языка
- ✅ Отображение флага текущего языка
- ✅ Открытие/закрытие выпадающего списка языков
- ✅ Переключение языка
- ✅ Закрытие выпадающего списка при клике вне его

### Бизнес-логика

#### `calculateTeamStats`

Тесты проверяют:

- ✅ Нулевая статистика для команды без игр
- ✅ Победа в основное время (3 очка)
- ✅ Победа в буллитах (2 очка)
- ✅ Поражение в основное время (0 очков)
- ✅ Поражение в буллитах (0 очков)
- ✅ Ничья в основное время (1 очко)
- ✅ Ничья в буллитах (1 очко)
- ✅ Статистика для гостевой команды
- ✅ Сложная статистика с несколькими играми
- ✅ Работа с разными типами ID (строки и числа)

### `calculateStandings`

Тесты проверяют:

- ✅ Пустой массив для пустого списка команд
- ✅ Сортировка команд по очкам
- ✅ Сортировка команд с одинаковыми очками по разнице голов
- ✅ Сортировка команд с одинаковыми очками и разницей по забитым голам
- ✅ Включение всех свойств команды в результат
- ✅ Правильный расчет статистики для всех команд

## 📊 Примеры запуска

### Успешный запуск

```bash
$ npm run test:run

> hockey-tournament@1.0.0 test:run
> vitest run

 RUN  v1.6.1

 ✓ src/utils/__tests__/calculateStats.test.js  (16 tests) 5ms

 Test Files  1 passed (1)
      Tests  16 passed (16)
```

### Запуск в watch режиме

```bash
$ npm test

> hockey-tournament@1.0.0 test
> vitest

  DEV  v1.6.1

 ✓ src/utils/__tests__/calculateStats.test.js  (16 tests) 5ms

 Test Files  1 passed (1)
      Tests  16 passed (16)
      Time  1.18s

 PASS  Waiting for file changes...
```

## 🔧 Настройка

Конфигурация тестов находится в `vite.config.js`:

```javascript
test: {
  globals: true,
  environment: 'jsdom',
  setupFiles: './src/test/setup.js',
}
```

- `globals: true` - делает функции `describe`, `it`, `expect` доступными глобально
- `environment: 'jsdom'` - использует jsdom для симуляции браузерного окружения
- `setupFiles` - файл с настройками, выполняемыми перед каждым тестом

## 📝 Написание новых тестов

1. Создайте файл `*.test.js` рядом с тестируемым модулем или в папке `__tests__`
2. Импортируйте функции для тестирования
3. Используйте `describe` для группировки тестов
4. Используйте `it` или `test` для отдельных тестов
5. Используйте `expect` для проверок

Пример:

```javascript
import { describe, it, expect } from 'vitest'
import { myFunction } from '../myModule'

describe('myFunction', () => {
  it('должен возвращать правильный результат', () => {
    const result = myFunction('input')
    expect(result).toBe('expected output')
  })
})
```

## 🐛 Отладка

### Запуск конкретного теста

```bash
npm test -- calculateTeamStats
```

### Запуск тестов в конкретном файле

```bash
npm test -- src/utils/__tests__/calculateStats.test.js
```

### Просмотр подробного вывода

```bash
npm test -- --reporter=verbose
```

## 📚 Дополнительные ресурсы

- [Документация Vitest](https://vitest.dev/)
- [Документация Testing Library](https://testing-library.com/)
- [Руководство по тестированию React](https://react.dev/learn/testing)

