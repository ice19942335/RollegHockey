# 🌐 Настройка кастомного домена для GitHub Pages

Эта инструкция поможет вам настроить домен `hockey.rolleg.eu` для вашего сайта на GitHub Pages.

## ✅ Что уже сделано

1. ✅ Создан файл `public/CNAME` с вашим доменом
2. ✅ Обновлен `vite.config.js` (убран префикс `/RollegHockey/`)
3. ✅ Обновлены файлы Chrome расширения на новый домен

## 🔧 Шаг 1: Настройка DNS у вашего провайдера домена

Вам нужно настроить DNS записи у провайдера, где зарегистрирован домен `rolleg.eu`.

**Для поддомена `hockey.rolleg.eu` используется CNAME запись.**

### Настройка CNAME записи для поддомена

Добавьте следующую DNS запись у вашего DNS провайдера:

**Тип записи:** `CNAME`  
**Имя/Хост:** `hockey` (или `hockey.rolleg.eu` в зависимости от интерфейса вашего провайдера)  
**Значение/Цель:** `ice19942335.github.io`  
**TTL:** `3600` (или значение по умолчанию)

**Пример настройки:**
```
Тип: CNAME
Имя: hockey
Значение: ice19942335.github.io
TTL: 3600
```

**Важно:**
- Для поддоменов **обязательно** используется CNAME запись
- A-записи для поддоменов не подходят для GitHub Pages
- Имя записи должно быть `hockey` (без `.rolleg.eu`), так как это поддомен

## 🔧 Шаг 2: Настройка домена в GitHub

1. Откройте ваш репозиторий на GitHub: https://github.com/ice19942335/RollegHockey
2. Перейдите в **Settings** (Настройки)
3. В левом меню найдите раздел **Pages**
4. В разделе **Custom domain** (Кастомный домен) введите: `hockey.rolleg.eu`
5. Нажмите **Save** (Сохранить)
6. GitHub автоматически проверит DNS настройки

**Важно:** 
- GitHub может потребовать некоторое время для проверки DNS (от нескольких минут до 24 часов)
- После успешной проверки появится галочка ✅ рядом с доменом
- GitHub автоматически создаст файл `CNAME` в репозитории (но мы уже создали его в `public/`, что тоже работает)

## 🔧 Шаг 3: Включение HTTPS (автоматически)

GitHub Pages автоматически предоставляет SSL сертификат для кастомных доменов через Let's Encrypt. После настройки DNS и домена в GitHub:

1. В настройках Pages включите опцию **Enforce HTTPS** (Принудительный HTTPS)
2. Это может занять некоторое время (до 24 часов) после настройки DNS

## ⏱️ Шаг 4: Ожидание распространения DNS

После настройки DNS записей нужно подождать:

- **Минимальное время:** 5-10 минут
- **Обычное время:** 1-2 часа
- **Максимальное время:** до 48 часов (в редких случаях)

Проверить распространение DNS можно с помощью:
- https://www.whatsmydns.net/#CNAME/hockey.rolleg.eu
- Или через командную строку: `nslookup hockey.rolleg.eu`

## 🚀 Шаг 5: Деплой изменений

После настройки DNS и домена в GitHub:

1. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Configure custom domain hockey.rolleg.eu"
   git push origin main
   ```

2. GitHub Actions автоматически задеплоит сайт

3. После деплоя ваш сайт будет доступен по адресу:
   ```
   https://hockey.rolleg.eu
   ```

## ✅ Проверка работы

После настройки проверьте:

1. **Доступность сайта:** Откройте `https://hockey.rolleg.eu` в браузере
2. **HTTPS:** Убедитесь, что сайт открывается через HTTPS (не HTTP)
3. **Все страницы работают:** Проверьте навигацию по сайту

## 🐛 Устранение проблем

### Проблема: Домен не работает, показывает ошибку DNS

**Решение:**
1. Проверьте правильность DNS записей у провайдера
2. Убедитесь, что прошло достаточно времени для распространения DNS (минимум 1 час)
3. Проверьте DNS через онлайн-сервисы (whatsmydns.net)

### Проблема: GitHub показывает ошибку проверки домена

**Решение:**
1. Убедитесь, что DNS записи настроены правильно
2. Подождите до 24 часов для полного распространения DNS
3. Проверьте, что файл `CNAME` существует в репозитории (GitHub может создать его автоматически)

### Проблема: Сайт открывается, но показывает 404

**Решение:**
1. Убедитесь, что `base: '/'` в `vite.config.js` (уже исправлено)
2. Проверьте, что деплой прошел успешно (вкладка Actions)
3. Очистите кэш браузера (Ctrl+Shift+R)

### Проблема: HTTPS не работает

**Решение:**
1. Подождите до 24 часов после настройки DNS
2. В настройках Pages включите "Enforce HTTPS"
3. Убедитесь, что DNS записи правильные

## 📝 Важные замечания

1. **Старый URL все еще работает:** `https://ice19942335.github.io/RollegHockey/` будет продолжать работать, но рекомендуется использовать новый домен

2. **Обновление Chrome расширения:** Если вы используете Chrome расширение, обновите его после деплоя, чтобы оно использовало новый URL

3. **Кэш браузера:** После переключения на новый домен может потребоваться очистка кэша браузера

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:
```
https://hockey.rolleg.eu
```

Все ссылки и навигация будут работать корректно с новым доменом.

---

**Последнее обновление:** 2025
