# 🏒 Турнирная таблица по хоккею

Веб-приложение для управления турнирной таблицей по хоккею с синхронизацией данных через Google Sheets.

## 🚀 Функции

- Управление командами (добавление, редактирование, удаление)
- Добавление и управление играми
- Автоматический подсчет турнирной таблицы
- Синхронизация данных с Google Sheets
- Интерактивное табло для отслеживания счета
- Система очков: 3-2-1-0

## 📋 Система очков

- Победа в основное время - 3 очка
- Победа в буллитах - 2 очка
- Ничья - 1 очко
- Поражение - 0 очков

## 🛠️ Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр билда
npm run preview
```

### Настройка Google Sheets

1. Создайте Google Таблицу
2. Следуйте инструкциям в файле `GOOGLE_SHEETS_SETUP.md`
3. Обновите `SPREADSHEET_ID` в `src/utils/googleSheets.js`
4. Настройте Google Apps Script для записи данных

## 📦 Деплой на GitHub Pages

### Автоматический деплой

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions. При каждом push в ветку `main` происходит автоматическая сборка и деплой.

### Ручной деплой

```bash
# Установите gh-pages (если еще не установлен)
npm install --save-dev gh-pages

# Соберите проект и задеплойте
npm run deploy
```

**Важно:** Перед деплоем убедитесь, что в `vite.config.js` указан правильный `base` путь (название вашего репозитория на GitHub).

## 📝 Структура проекта

```
├── src/
│   ├── components/      # React компоненты
│   ├── utils/          # Утилиты (Google Sheets, расчеты)
│   ├── constants/      # Константы (цвета, логотипы)
│   ├── App.jsx         # Главный компонент
│   └── main.jsx        # Точка входа
├── .github/
│   └── workflows/      # GitHub Actions workflows
├── GOOGLE_SHEETS_SETUP.md  # Инструкция по настройке Google Sheets
└── package.json
```

## 🔧 Технологии

- React 18
- Vite
- Google Sheets API
- CSS3

## 📄 Лицензия

MIT


